import { Text } from '@chakra-ui/react';

export const PickPositionsText = () => {
    return (
        <Text fontSize="lg" mb={4} color="primary" fontWeight="bold">
            PICK YOUR POSITIONS
        </Text>
    );
};
